apiVersion: apps/v1
kind: Deployment
metadata:
  name: mydeployment
spec:
  selector:
    matchLabels:
      app: dw
  template:
    metadata:
      labels:
        app: dw
    spec:
      containers:
      - name: dw
        image: <Image>
        ports:
        - containerPort: <Port>
